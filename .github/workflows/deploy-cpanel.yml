name: Deploy to cPanel

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: s490.v2nets.com
          username: proyec19
          port: 2200
          key: ${{ secrets.CPANEL_SSH_KEY }}
          script: bash /home/proyec19/tareas_app/scripts/deploy.sh
